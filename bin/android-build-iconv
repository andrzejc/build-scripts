#! /usr/bin/env bash
set -e -o pipefail 
set +x

THIS_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
SOURCE_URL="${SOURCE_URL:-https://ftp.gnu.org/pub/gnu/libiconv/libiconv-1.16.tar.gz}"
source "${THIS_DIR}/android-autotools-build.sh"
