#! /usr/bin/env bash
set -e -o pipefail
set +x
# optional dependencies:
THIS_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
SOURCE_URL="${SOURCE_URL:-https://archive.mozilla.org/pub/opus/opus-1.3.1.tar.gz}"
source "${THIS_DIR}/android-autotools-build.sh" \
    --disable-extra-programs
